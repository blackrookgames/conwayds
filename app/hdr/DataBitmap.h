#ifndef DATABITMAP_H
#define DATABITMAP_H

#include <nds.h>

class DataBitmap
{
    public:

    static const size_t palette_size;
    static const u8 palette_data[];
    static const size_t bitmap_size;
    static const u8 bitmap_data[];
};

#endif
