#ifndef DATACHAMP_H
#define DATACHAMP_H

#include <nds.h>

class DataChamp
{
    public:

    static const size_t palette_size;
    static const u8 palette_data[];
    static const size_t tileset_size;
    static const u8 tileset_data[];
    static const size_t tilemap_size;
    static const u8 tilemap_data[];
};

#endif
